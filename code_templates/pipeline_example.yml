# Example pipeline snippet for data processing and model training

steps:
  - script: |
      python -m venv venv
      source venv/bin/activate
      pip install -r requirements.txt
      python scripts/prepare_data.py
      python scripts/train_model.py
    displayName: 'Train model'
